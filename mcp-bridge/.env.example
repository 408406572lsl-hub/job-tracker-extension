# 天眼AI (tianyancha.com/ai) 开放 MCP 接入凭证模板
#
# 【重要】本文件只是模板,复制为 .env 后填写你自己的 Key。
#   cp .env.example .env
#   # 然后编辑 .env,填入你从 https://tianyancha.com/ai 申请的 API Key
#
# 不想用 .env 也行:在「岗位猎手」扩展的 设置页 → 「企业分析(天眼查)」 卡片里
# 直接填入 Key,插件会在桥接握手时把 Key 推给本桥接进程(优先级高于 .env)。
# 这样 Key 只存在你本地浏览器,不涉及本文件,适合打包分发给他人使用。
#
# 安全:真实 .env 已被 .gitignore 排除,绝不进 git / 扩展包。开源时只提交本 .env.example。

# 你的天眼AI API Key(必填;留空则企业分析不可用)
TYC_API_KEY=

# 天眼AI MCP 端点(一般无需改动)
TYC_MCP_ENDPOINT=https://mcp.tianyancha.com/v1
